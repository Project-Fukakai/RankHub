# ❌ 已归档：这条流水线**不再迁移**，保留原文仅供追溯。
#
# 归档理由（2026-09-18 迁移到 GitHub Actions 时定）：
#   它是**一次性的能力探针**（探「options 能不能传 --target / --build-arg」「平台构建后端
#   能不能从镜像站拉 Docker Hub 基础镜像」），结论已经写进下面的注释与
#   docs/CI_CD_PIPELINE.zh-CN.md，探针本身没有持续价值。搬过去只会多一份会过期的定义。
#
# 本文件用 `.yml.disabled` 后缀：GitHub 只加载 `*.yml` / `*.yaml`，所以它**不会被调度**，
# 也不会被 .github/workflows/validate-workflows.yml 的结构检查当作在跑的 workflow。
#
# 原始位置：Codeup `RankHub-NET` 的 `.yunxiao/rankhub-probe.yml`（源仍在那里，未删除）。
# ═══════════════════════════════════════════════════════════════════════════════
#
# .yunxiao/rankhub-probe.yml
#
# v13：**结论已反转，现在补最后一块拼图** —— options 能不能传 --target / --build-arg，
#      以及平台构建后端能不能从镜像站拉 Docker Hub 基础镜像。
#
# ══════════════════════════════════════════════════════════════════════════
# ❗❗ 重大修正：**公共构建集群可以构建并推送镜像到腾讯云 TCR**
#    （runId 11 已完全证实；我前面 11 轮的否定结论作废）
# ══════════════════════════════════════════════════════════════════════════
# 我错在哪：一直在用 **`Command` 步骤自己跑 docker**。容器确实缺 CAP_SYS_ADMIN
#   （`docker pull` → `unshare: operation not permitted`；`docker build` → `mount … 失败`），
#   所以怎么试都不通。但平台的 docker 能力是**按步骤供给**的 —— 用官方镜像构建步骤时，
#   平台会为该步骤准备可用的环境。runId 11 的 `DockerBuildPush` 日志即铁证：
#     Docker daemon is ready
#     Docker buildx builder not installed, installing...
#     #1 pulling image build-steps-public-registry…/build-steps/docker@sha256:c3cb…
#     #1 creating container buildx_buildkit_flow-build-container0
#     #0 building with "flow-build-container" instance using docker-container driver
#     #4 [1/1] COPY package.json /pkg.json   DONE
#     #5 exporting manifest sha256:1abc… / manifest list sha256:4db28d4d…
#     #6 [auth] rankhub/rankhub-probe:pull,push token for ccr.ccs.tencentyun.com   ← 复用了我上一步的 docker login
#     #5 pushing layers 1.6s done
#     #5 pushing manifest for ccr.ccs.tencentyun.com/rankhub/rankhub-probe:flowtest@sha256:4db28d4d… 1.1s done
#   ⇒ 三条同时成立：① 平台构建后端工作；② `Command` 里的 `docker login`（凭据来自加密变量组）
#     能被该步骤复用；③ 能推到 TCR。**并且 `rankhub` 命名空间是存在的**
#     （此前 `rankhub-backend` 报 not found 只是因为那个仓库还没建）。
#
# 【步骤 ID 实测（用 YAML 校验器反问，免费）】
#   ✅ `DockerBuildPush`      必填 artifact / image / dockerfilePath
#                            可选 contextPath / cacheType / cacheImageId / options
#                            **没有凭据参数** —— 官方把 login 与 build 解耦成两类步骤
#   ✅ `DockerBuildPushACR`   必填 serviceConnection / artifact / region /
#                            dockerRegistry / dockerTag / dockerfilePath
#   ✅ `DockerLogin`          必填 dockerRepository / **serviceConnection**
#   ❌ `DockerBuildPushCustom` 不存在（校验器明确报 `未找到此step`）
#
# 【为什么登录用 Command 而不是 DockerLogin】
#   `DockerLogin` 必填 `serviceConnection`，而云效的服务连接类型里**没有腾讯云 TCR**
#   （只有 ACR / Harbor 等）⇒ 用不了。改用 `Command` + `docker login`（加密变量组凭据），
#   已实测能被 DockerBuildPush 复用。
#
# ══════════════════════════════════════════════════════════════════════════
# v13 要验的两件事（这是本仓库能否照搬的最后一块拼图）
# ══════════════════════════════════════════════════════════════════════════
# ① **`options` 能否传 `--target`**：本仓库根 Dockerfile 有 backend/frontend/admin
#    三个 target，而 `DockerBuildPush` 只接受 `dockerfilePath`、**没有 target 参数**。
#    若 `--target` 能塞进 `options`，§6.2「内置步骤表达不了多 target」这条限制就被推翻。
# ② **能否从镜像站拉 Docker Hub 基础镜像**：用 `--build-arg BASE_NODE=<镜像站>` 把
#    `FROM ${BASE_NODE}` 指向 `docker.m.daocloud.io/library/alpine:3.20`（已实测可下载 blob）。
#    这同时验证「镜像站可达」与「--build-arg 生效」。
# 测试用多 stage 的迷你 Dockerfile（不碰业务 Dockerfile，避免跑十几分钟的 pnpm install）：
#    base → svc-a / svc-b，分别用 --target 构建，看是否只产出目标 stage 的内容。
#
# 若两项都过 ⇒ **§6.2 的整条流水线可以照搬，唯一改动是把 `Command`+bake 换成
#   `DockerBuildPush` 步骤**（每个 target 一个步骤，cacheType=remote 复用 deps 层）。
#
# ══════════════════════════════════════════════════════════════════════════
# 仍然成立的结论（与本次实验无关）
# ══════════════════════════════════════════════════════════════════════════
# * **`Command` 里自己跑 docker 不行**：容器缺 `CAP_SYS_ADMIN`（CapEff=00000000a80425fb），
#   实测 `unshare` / `mount` 均被内核拒。**但用官方构建步骤就行**（见上）。
# * 平台用 **`/bin/bash` 作容器入口** ⇒ 自定义镜像必须有 bash（Alpine 系 dind 直接被拒）。
# * 平台预设 **`DOCKER_HOST=tcp://127.0.0.1:2375` 但无人监听** ⇒ 判断「有没有 docker」
#   时最大的干扰项。
# * Docker Hub 不可达，但**镜像站可解**（4 家全量可用、digest 与上游一致）。
#   ⚠️ 阿里云 `registry.cn-beijing.aliyuncs.com/hub-mirrors/*` **已报废**
#      （node 最新才 13.x、python 只有 3.7、ubuntu 只有 trusty），不能用。
# * 5 个 stage 里 4 个不需要 docker；构建容器运行时 python3 3.6.8 / jq 1.6 / 无 node。
#
# ══════════════════════════════════════════════════════════════════════════
# 下方为云效 YAML 原文（不是 GHA 语法，仅为留档；不要照它写 GHA）
# ══════════════════════════════════════════════════════════════════════════
#
# name: rankhub-probe-docker
#
# sources:
#   main_repo:
#     type: codeup
#     name: RankHub-NET
#     endpoint: https://codeup.aliyun.com/stcn/RankHub/RankHub-NET.git
#     branch: main
#     cloneDepth: 1
#     # 刻意不配 triggerEvents：本流水线只手动跑
#     certificate:
#       type: serviceConnection
#       serviceConnection: jpmaaa1bsk6dz899
#
# defaultWorkspace: main_repo
#
# stages:
#   probe_stage:
#     name: options 能力 + 镜像站基础镜像
#     jobs:
#       target_probe_job:
#         name: --target / --build-arg / 镜像站 三合一实测
#         runsOn:
#           group: public/cn-beijing
#           container: build-steps-public-registry.cn-beijing.cr.aliyuncs.com/build-steps/alinux3:latest
#           instanceType: LARGE_4C8G
#         timeoutMinutes: 30
#         driven: auto
#         plugins: []
#         steps:
#           prep:
#             name: 写多 stage 测试 Dockerfile
#             step: Command
#             with:
#               run: |
#                 set +e
#                 set -uo pipefail
#                 echo "### 登录 TCR（凭据来自加密变量组）"
#                 printf '%s' "${TCR_PASSWORD:-}" | docker login "${REGISTRY:-ccr.ccs.tencentyun.com}" \
#                   -u "${TCR_USERNAME:-}" --password-stdin 2>&1 | tail -2
#
#                 echo
#                 echo "### 写多 stage Dockerfile.probe（base → svc-a / svc-b）"
#                 cat > Dockerfile.probe <<'EOF'
#                 ARG BASE_NODE=node:22-bookworm-slim
#                 FROM ${BASE_NODE} AS base
#                 RUN echo "=== 基础镜像 OK ===" && cat /etc/os-release | head -2
#                 FROM base AS svc-a
#                 RUN echo a > /a.txt
#                 FROM base AS svc-b
#                 RUN echo b > /b.txt
#                 EOF
#                 cat Dockerfile.probe
#                 exit 0
#
#           build_a:
#             name: 构建 svc-a（--target + --build-arg 镜像站）
#             step: DockerBuildPush
#             with:
#               artifact: probe_a
#               image: ccr.ccs.tencentyun.com/rankhub/rankhub-probe:svc-a
#               dockerfilePath: Dockerfile.probe
#               cacheType: no-cache
#               options: --target svc-a --build-arg BASE_NODE=docker.m.daocloud.io/library/alpine:3.20
#
#           build_b:
#             name: 构建 svc-b（只用 --target，验证 default BASE_NODE 不可达时的表现）
#             step: DockerBuildPush
#             with:
#               artifact: probe_b
#               image: ccr.ccs.tencentyun.com/rankhub/rankhub-probe:svc-b
#               dockerfilePath: Dockerfile.probe
#               cacheType: no-cache
#               options: --target svc-b --build-arg BASE_NODE=docker.m.daocloud.io/library/alpine:3.20
